# brain_region_manifest.yml — Neuro-anatomical file routing (seed)
# Auto-generated/refreshed by `scripts/memory_region_manifest_build.py`
# Manual edits here are overwritten — for pinning/exclusion use brain_region_overrides.yml
# Initialized: {{DATE}}
# Schema version: 1
schema_version: 1
generated_at: "{{DATE}}T00:00:00+00:00"

# ── Seed mapping ────────────────────────────────────────────────────
# After first run of memory_region_manifest_build.py this file is rewritten
# from the live workspace. The seed below illustrates the intended layout
# so the agent can navigate even before the first refresh tick.
regions:
  hippocampus:
    display: "Hippocampus — encoding, episodic memory"
    core_files:
      - { path: "MEMORY.md" }
      - { path: "memory/fact.yml" }
      - { path: "memory/episodic.jsonl" }
    on_demand_files:
      - { path: "CLAUDE.md" }
      - { path: "memory/consolidations.jsonl" }
      - { path: "memory/associations.jsonl" }

  prefrontal:
    display: "Prefrontal — execution, planning, tools"
    core_files:
      - { path: "AGENTS.md" }
      - { path: "memory/fact_governance.yml" }
    on_demand_files:
      - { path: "memory/fact_decisions.yml" }
      - { path: "memory/fact_archive.yml" }

  limbic:
    display: "Limbic — emotion, personality, preferences"
    core_files:
      - { path: "SOUL.md" }
      - { path: "USER.md" }
    on_demand_files: []

  amygdala:
    display: "Amygdala — vigilance, taboos, rules"
    core_files:
      - { path: "_Agent_System/99_System/990_POLICY/ACCESS_POLICY.md" }
      - { path: "_Agent_System/99_System/990_POLICY/AUTONOMY_POLICY.md" }
    on_demand_files: []

  parietal:
    display: "Parietal — space, paths, structure"
    core_files: []
    on_demand_files: []

  temporal:
    display: "Temporal — knowledge, semantics"
    core_files: []
    on_demand_files: []

# ── Refresh policy ──────────────────────────────────────────────────
# Run `bash scripts/memory_region_refresh_tick.sh` to rebuild this file.
# Defaults: weekly refresh; pin/exclude via brain_region_overrides.yml.
refresh:
  enabled: true
  interval_days: 7
  last_run: null
