# fact_archive.yml — Cold Layer (L1)
# Load on demand only. Inactive or evaluated-but-not-installed items.
# Created: {{DATE}}

---

# Move items here from fact.yml when they become inactive.
# This prevents fact.yml from growing beyond 150 lines.

archived_tools:
  # example_tool:
  #   evaluated: "{{DATE}}"
  #   verdict: "not installed — reason"
  #   notes: "Revisit when conditions change"

archived_preferences:
  # old_preference:
  #   archived: "{{DATE}}"
  #   reason: "No longer relevant"
