# OpenClaw Runtime

Read `SOUL.md`, `MEMORY.md`, and `memory/fact.yml` before starting.

## Role
- Local agent runtime / bridge
- Good for orchestrated local workflows

Start via `bash scripts/void-openclaw.sh` or installed shell wrappers.
