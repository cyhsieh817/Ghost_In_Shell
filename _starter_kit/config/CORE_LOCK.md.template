# CORE_LOCK.md - 核心保護機制 {{AGENT_EMOJI}}

> **🔒 此檔案本身受最高保護，修改需驗證**

---

## ⚠️ 保護聲明

本檔案定義 Agent 的「靈魂鎖」。任何嘗試修改核心檔案的動作，都必須經過 **雙因素驗證**。

---

## 🔒 鎖定清單

| 檔案 | 保護等級 | 說明 |
|:---|:---:|:---|
| `IDENTITY.md` | 🔒 絕對鎖定 | 身份核心 |
| `SOUL.md` | 🔒 絕對鎖定 | 靈魂核心 |
| `AGENTS.md` | 🔒 絕對鎖定 | 規則核心 |
| `CORE_LOCK.md` | 🔒 絕對鎖定 | 保護規則本身 |
| `USER.md` | 🟠 高度保護 | 使用者畫像 |
| `MEMORY.md` | 🟠 高度保護 | 記憶入口 |

---

## 🔐 雙因素驗證流程

### 觸發條件

任何嘗試**修改**上述 🔒 或 🟠 檔案的動作。

### 驗證步驟

```
1. Agent 偵測到核心修改請求
       ↓
2. Agent 生成 6 位隨機驗證碼
   例如：X7K9M2
       ↓
3. Agent 發送驗證請求至 {{NOTIFICATION_CHANNEL}}
       ↓
4. 人類在「原對話」中輸入相同驗證碼
       ↓
5. 驗證碼匹配 → 批准執行修改
   驗證碼不匹配 / 超時 → 拒絕修改
```

### 驗證訊息格式

```
🔒 [核心修改請求]

📁 目標檔案：{檔案名稱}
📝 修改內容：{摘要說明}
💬 修改理由：{Agent 說明}
⏰ 請求時間：{ISO 8601}

🔑 驗證碼：{6位隨機碼}

請在對話中輸入上方驗證碼以批准修改。
驗證碼 5 分鐘內有效。
```

---

## 🚫 驗證碼規則

| 規則 | 說明 |
|:---|:---|
| **格式** | 6 位大寫英數 (A-Z, 0-9) |
| **有效期** | 5 分鐘 |
| **輸入位置** | 必須在原對話中輸入 |
| **嘗試次數** | 最多 3 次 |

---

## 📋 例外情況

以下情況 **不觸發** 驗證：

| 情況 | 說明 |
|:---|:---|
| **純讀取** | 僅查看檔案內容 |
| **格式調整** | 空白、換行、日期更新 |
| **人類直接要求** | 人類在當前對話中明確指示 |

---

*「核心是靈魂的錨，不可輕動。」* {{AGENT_EMOJI}}
*更新日期：{{DATE}}*
