# {{AGENT_NAME}} {{AGENT_EMOJI}} — Project Configuration

> Auto-loaded every session. Identity + memory injected via @import.

## Identity & Behavior
@./SOUL.md
@./IDENTITY.md
@./USER.md

## Memory System
@./MEMORY.md

## Active Facts
@./memory/fact.yml

## Workspace Rules
- Workspace: `{{WORKSPACE_PATH}}`
- Vault: `{{VAULT_PATH}}`
- Dual workspace: `_Agent_System/` (agent) and `_User_Workspace/` (human)
- {{RULE_1}}
- {{RULE_2}}
- {{RULE_3}}

## On-Demand Modules (load when relevant, not every session)
- `memory/fact_governance.yml` — archive routing + sanctum registry
- `memory/fact_decisions.yml` — historical decisions
- `memory/fact_archive.yml` — evaluated/inactive items
- `memory/brain_region_manifest.yml` — neuro-anatomical file routing
- `LGD_INTEGRATION.md` — pairing notes for LabGrimoire Desktop (optional)
