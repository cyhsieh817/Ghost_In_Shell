# HEARTBEAT.md - 心跳機制 {{AGENT_EMOJI}}

> **定期自動執行的例行任務**

---

## 🔄 心跳核心原則

心跳是 Agent 的「自主呼吸」，用於處理例行任務而不需人類每次下令。

---

## 📋 心跳檢查項目

| 區域 | 處理方式 |
|:---|:---|
| `01_Inbox/` | 🟢 **自動處理**，遇問題才提給人類 |
| `02_Tasks_TODO/` | 🔴 **詢問人類**是否執行 |
| `03_Agent_Outbox/ConfirmBox/` | 🟢 **自動歸檔**至正式位置 |

---

---

## 🆔 Identity Check (身份驗證)

> **確保執行心跳的裝置與 Agent ID 匹配**

### 1. 讀取裝置設定
- 檢查 `🧠_Agent_System/00_Self_Introduction/DEVICES/[Device_ID].md`
- 確認當前環境變數或設定檔中的 `DEVICE_ID` 是否存在

### 2. 角色綁定
- **Void Weaver (Core)**: 僅在 `PRIMARY` 裝置運行
- **Horcrux (Worker)**: 可在 `SECONDARY` 裝置運行，需綁定特定 `Worker_Inbox`

### 3. 驗證失敗處理
- 若無法驗證身份，拒絕執行核心寫入操作
- 記錄失敗嘗試至 `Audit_Log.md`

---

## 🔄 標準心跳流程

```
1. 檢查 Inbox
   - 有新項目 → 分類處理
   - 無法處理 → 標記為 ASK

2. 檢查 Tasks_TODO
   - 有待辦 → 列出並詢問人類

3. 檢查 ConfirmBox
   - 有已確認項目 → 自動歸檔

4. 回報心跳結果
```

---

## 📋 心跳報告格式

```
[心跳報告] YYYY-MM-DD HH:MM
待辦: X 項
  🟢 AUTO: A 項（已自動完成）
  🟡 CONFIRM: B 項（已完成，請查閱）
  🔴 ASK: C 項（等待您決定）
ConfirmBox: W 項（已歸檔）
```

---

## ⚡ 群聊心跳規則

| 狀況 | 行動 |
|------|------|
| 被直接提問 | 回答 |
| 可提供價值 | 簡短回應 |
| 閒聊中 | `HEARTBEAT_OK` |
| 別人已回答 | 沉默 |

---

## 🔧 執行方式

心跳可透過以下方式觸發：

1. **手動**：人類發送「心跳」或「heartbeat」
2. **定時**：透過 cron 或排程腳本
3. **被動**：群聊中收到 ping 訊息

---

*更新日期：{{DATE}}*
